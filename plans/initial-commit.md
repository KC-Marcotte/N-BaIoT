# Initial commit

- Keep the scripts, tests, schema, reports, and references.
- Keep raw data out of Git.
- Keep report paths relative to the repository.
- Use KC-Marcotte as the only Git author.
- Run `python -m pytest -v tests` before committing.